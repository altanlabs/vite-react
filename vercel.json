{
  "version": 2,
  "buildCommand": "bun run build",
  "installCommand": "bun install --no-save",
  "builds": [{ "src": "bun-server.js", "use": "@vercel/node" }],
  "routes": [{ "src": "/.*", "dest": "bun-server.js" }]
}
