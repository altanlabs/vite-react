# Ensure platform-specific binaries are installed correctly
@swc:core_binary_host_mirror=https://registry.npmjs.org/@swc/core/-/

# For Vercel deployment
swc_platform=linuxmusl-x64

# For local development (uncomment if needed)
# swc_platform=darwin-x64
